# Success Criteria

The project will be considered successful if:

- The model predicts churn with reasonable accuracy
- The deployed application works correctly
- Both single and batch prediction are supported
- The application is accessible through a web interface